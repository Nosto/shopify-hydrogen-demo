export function NostoItem({recommendation}) {
  let {title, result_id, products} = recommendation;

  return (
    <div className="nosto-container">
      <div className="nosto-title"></div>
    </div>
  );
}
