import * as React from 'react';
export function NostoOther(props) {
    React.useEffect(() => {
        let cancelled = false;
        import('@nosto/nosto-react').then((m) => {
            if (!cancelled)
                m.useNostoOther(props);
        });
        return () => {
            cancelled = true;
        };
    }, [props]);
    return null;
}
