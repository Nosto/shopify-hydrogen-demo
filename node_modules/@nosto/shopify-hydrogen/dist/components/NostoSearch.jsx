import * as React from 'react';
export function NostoSearch(props) {
    React.useEffect(() => {
        let cancelled = false;
        import('@nosto/nosto-react').then((m) => {
            if (!cancelled)
                m.useNostoSearch(props);
        });
        return () => {
            cancelled = true;
        };
    }, [props]);
    return null;
}
