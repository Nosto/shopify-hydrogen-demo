import * as React from 'react';
export function NostoHome(props) {
    React.useEffect(() => {
        let cancelled = false;
        import('@nosto/nosto-react').then((m) => {
            if (!cancelled)
                m.useNostoHome(props);
        });
        return () => {
            cancelled = true;
        };
    }, [props]);
    return null;
}
