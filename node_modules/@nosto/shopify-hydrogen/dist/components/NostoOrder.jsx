import * as React from 'react';
export function NostoOrder(props) {
    React.useEffect(() => {
        let cancelled = false;
        import('@nosto/nosto-react').then((m) => {
            if (!cancelled)
                m.useNostoOrder(props);
        });
        return () => {
            cancelled = true;
        };
    }, [props]);
    return null;
}
