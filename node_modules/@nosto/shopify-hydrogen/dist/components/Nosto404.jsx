import * as React from 'react';
export function Nosto404(props) {
    React.useEffect(() => {
        let cancelled = false;
        import('@nosto/nosto-react').then((m) => {
            if (!cancelled)
                m.useNosto404(props);
        });
        return () => {
            cancelled = true;
        };
    }, [props]);
    return null;
}
