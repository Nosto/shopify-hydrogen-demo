import * as React from 'react';
export function NostoCategory(props) {
    React.useEffect(() => {
        let cancelled = false;
        import('@nosto/nosto-react').then((m) => {
            if (!cancelled)
                m.useNostoCategory(props);
        });
        return () => {
            cancelled = true;
        };
    }, [props]);
    return null;
}
